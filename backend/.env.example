# Neon PostgreSQL Connection String (or local Postgres)
# Example: postgresql://user:password@ep-xyz.us-east-2.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

# If DATABASE_URL is left blank or unset, the backend automatically falls back to:
# sqlite:///./regulatory_fabric.db for instant zero-dependency local running!
